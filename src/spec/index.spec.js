import {assert} from 'chai';
import findCycles from '../index.js';

suite('API', () => {
  test('should be a function', () => {
    assert(findCycles).isFunction();
  });
  
  test('should be a function', () => {
    assert(findCycles).isFunction();
  });
});